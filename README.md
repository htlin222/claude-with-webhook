# claude-with-webhook

GitHub Issue → Claude Code automation via webhookd + Tailscale Funnel.

When a specified user opens an issue, Claude auto-plans and replies. On approval, Claude implements in a worktree, commits, pushes, and opens a PR.
